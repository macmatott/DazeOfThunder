# Discord bot
discord.py>=2.4.0

# Supabase client — bot talks to Supabase, never touches scoring logic
# or writes its own SQL. Reads via the same tables/views the website uses.
supabase>=2.6.0
httpx>=0.27.0

# Config
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Dev tooling
black>=24.4.0
ruff>=0.5.0
